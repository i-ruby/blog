spring:
  devtools:
    restart:
      enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://121.4.73.4:3306/blog
    username: root
    password: iruby234
    
logging:
  level: {work.iruby.blog.mapper: debug}

mybatis-plus:
  global-config:
    db-config:
      id-type: auto
  mapper-locations: classpath*:/mybatis/xml/*.xml
